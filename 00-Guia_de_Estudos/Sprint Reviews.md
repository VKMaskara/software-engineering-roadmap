# Sprint Review — Semana 01

## 📚 Etapa 1 — Revisão das Anotações

### O que você quis dizer aqui?

**Aula 01** -> Quis dizer que ser um bom programador está na capacidade de quebrar um problema maior em pequenos passos lógicos.

**Aula 02** -> Durante a aula fica claro como a variável é a base da programação, pois tudo o que fazemos envolve armazenar informações, modificá-las e utilizá-las para resolver problemas.

**Aula 03** -> Que operadores são símbolos, sejam eles aritméticos, relacionais ou lógicos, com a função de realizar uma operação em um ou mais valores, e são eles que permitem a manipulação das informações armazenadas.

**Aula 04** -> Estruturas de decisão são como o computador decide qual caminho pré-definido seguir com base em condições. Como se fossem perguntas de sim ou não que o algoritmo faz para entender para onde seguir.

Se sim -> Faça isso.

Se não -> Faça outra coisa.

---

### Você ainda escreveria essa definição dessa forma?

**Aula 01** -> Sim, é essencial que entendamos o problema e o quebremos em partes. Um bom programador é aquele que resolve o problema.

**Aula 02** -> Sim, no meu exemplo eu pensei nas variáveis como gavetas em um armário que podem receber um tipo de roupa, assim como uma variável recebe um tipo de dado.

**Aula 03** -> Sim, os operadores são símbolos que nos permitem manipular dados armazenados.

**Aula 04** -> Sim, aqui fica claro que a estrutura de decisão são perguntas que o algoritmo faz e segue um caminho diferente para sim ou não, como se fosse uma bifurcação na via.

---

### Esse exemplo ainda faz sentido?

**Aula 01** -> O exemplo da aula não só faz sentido, como agora observo que seguimos diversos algoritmos durante o dia a dia.

**Aula 02** -> Sim, faz total sentido, pois é uma analogia a algo do dia a dia.

**Aula 03** -> Sim, os exemplos matemáticos, relacionais e lógicos são exemplos que conseguimos visualizar facilmente de forma nítida.

**Aula 04** -> Sim, os exemplos da aula foram bem lúdicos.

---

### Esse exemplo ainda faz sentido?

**Aula 01** -> Sim, a analogia que eu fiz não é só com uma receita de bolo, mas com todos os passos a passo que seguimos durante o dia.

**Aula 02** -> Acredito que essa é a forma mais didática.

**Aula 03** -> Operadores são ferramentas que realizam operações sobre os dados armazenados.

**Aula 04** -> Acredito que essa é a forma mais didática.

---

## 🧠 Etapa 2 — Revisão Ativa

### O que é um algoritmo?

-> Algoritmo é a quebra de um problema maior em pequenos passos, criando assim um passo a passo finito e com uma sequência lógica a fim de resolver o problema.

### Qual a diferença entre variável e constante?

-> Variável pode ser alterada durante a execução do código.

-> Constante mantém o seu valor durante toda a execução do código.

### Por que existe o operador %?

O operador `%` representa o resto da divisão. Podemos utilizá-lo, por exemplo, para identificar números pares e ímpares.

### Qual a diferença entre && e ||?

**&&** -> Operador lógico em que as duas condições precisam ser verdadeiras.

**||** -> Operador lógico em que apenas uma condição precisa ser verdadeira.

### O que é uma decisão composta?

-> Uma decisão composta é quando definimos no código um caminho para o caso da condição ser verdadeira e outro para o caso dela ser falsa.

---

## 📝 Etapa 3 — Desafio Integrador

Este está como desafio final no arquivo **🚀 Desafio.md** da pasta.

---

## 📈 Etapa 4 — Sprint Review

### O que você aprendeu nesta semana?

- Algoritmos: o que são e como construí-los.
- Diferença entre variáveis, constantes e tipos de dados.
- Operadores.
- Estruturas de decisão.

### O que foi mais difícil?

- Pensar como um computador para criar um algoritmo detalhado.

### O que você faria diferente?

- Nada, estou gostando da estrutura e estou entendendo. Agora é só aprofundar.

### Qual assunto merece revisão daqui a um mês?

- Acredito que projetos que fortaleçam minha capacidade de criar algoritmos.

### O que você percebeu sobre sua evolução?

- Que não estou apenas copiando ou lendo; estou criando uma base sólida e documentando esse processo.

---

## 📈 Etapa 5 — Sprint Review

# Sprint Review — Semana 01

## O que aprendi

- Algoritmos.
- Variáveis.
- Operadores.
- Estruturas de decisão.

---

## O que tive dificuldade

- Pensar no problema por completo.

---

## O que preciso revisar

- Operador módulo.

---

## O que mais gostei

- Estruturas de decisão.

---

## Próximos objetivos

- Aprender teste de mesa.
- Aprender estruturas de repetição.
- Continuar documentando a jornada.

# Sprint Review — Semana 02 

## 🧠 Etapa 1 — Revisão das Anotações

### Teste de Mesa

**O que é um Teste de Mesa?**

-> Teste de Mesa é um método de leitura e execução manual de um código, basicamente a leitura e a interpretação de um algoritmo linha por linha.

**Qual é o objetivo de fazer um Teste de Mesa antes de executar um algoritmo?**

-> Entender o seu fluxo de execução e lógica para verificar e evitar a existência de erros.

**Qual foi a maior dificuldade que você encontrou ao fazer Testes de Mesa?**

=> Não tive grandes dificuldades. É basicamente pensar como se só coubesse uma única informação dentro de um espaço de memória (variável), e para uma entrar, a outra tem que ser sobrescrita.

---

### Fluxo de Execução

**O que é Fluxo de Execução?**

=> Fluxo de Execução é como se fosse um mapa de quais caminhos o nosso algoritmo deve percorrer com base nas condições. Se a condição for `true`, execute isso; se não, faça aquilo.

**Qual a diferença entre Fluxo de Execução e Teste de Mesa?**

=> A diferença está nas perguntas que eles respondem.

- **Teste de Mesa:**
  R: Que valor deveria estar nessa variável?

- **Fluxo de Execução:**
  R: Qual caminho foi percorrido até aqui?

**Por que o algoritmo continua executando após sair de um bloco `if/else`?**

- Pois o computador só para de executar quando o algoritmo acaba, e não quando o bloco `if/else` termina. Quando ele termina, é apenas aquele bloco que acabou.

---

### Depuração Manual

**O que significa depurar um algoritmo?**

-> É o ato de investigar e analisar o código a fim de encontrar e corrigir erros.

**Quais são os três principais tipos de erro que estudamos?**

-> Sintaxe, lógica e execução.

**Como o Teste de Mesa e o Fluxo de Execução ajudam na depuração?**

-> Eles nos ajudam a entender quais valores foram armazenados e com quais dados o computador está trabalhando, o que nos leva a compreender qual caminho está sendo percorrido.

---

## ⚡ Etapa 2 — Revisão Ativa

**Se uma variável muda de valor durante a execução, qual ferramenta ajuda a acompanhar essa mudança?**

- Teste de Mesa.

**Se um algoritmo segue o caminho errado, qual conceito ajuda a identificar isso?**

- Fluxo de Execução.

**Qual é a diferença entre um erro de sintaxe e um erro de lógica?**

Um erro de sintaxe é um erro de escrita no código. Um erro de lógica é quando o sistema não funciona como deveria por não seguir a regra de negócio.

**Por que um loop infinito é considerado um problema?**

- Porque isso pode travar navegadores, sistemas operacionais ou softwares.

**O que significa dizer que um algoritmo possui um fluxo sequencial?**

-> Ele é um algoritmo simples, sem a existência de condicionais, e segue uma linha reta de execução.

---

## 🚀 Etapa 3 — Desafio Integrador da Semana

Imagine um sistema de matrícula escolar com as seguintes regras:

- O aluno deve estar cadastrado.
- Deve ter a mensalidade em dia.
- Deve possuir vaga disponível na turma.
- Não pode estar bloqueado.

Um desenvolvedor escreveu o seguinte algoritmo:

```javascript
let cadastro = true
let mensalidade = true
let vaga = true
let bloqueado = true

if (cadastro && mensalidade && vaga){
    console.log("Matrícula liberada")
}else{
    console.log("Matrícula negada")
}

console.log("Registro da tentativa realizado")
```

Sem alterar o código inicialmente, responda:

**Qual é o erro presente no algoritmo?**

A variável `bloqueado` não está inserida na condicional, permitindo que alunos bloqueados tenham a matrícula liberada.

**Classifique o tipo de erro.**

Lógico, pois não segue a regra de negócio.

**Faça um Teste de Mesa.**

-> As variáveis seguem com o mesmo valor do começo ao fim.

**Descreva o Fluxo de Execução.**

```text
Início
  |
  v
Declara as variáveis
  |
  v
Possui cadastro && mensalidade && vaga?
      |                                        |
     Sim                                      Não
      |                                        |
      v                                        v
 Matrícula liberada // Executado   Matrícula negada // ignorado
        \           /
         \         /
          v       v
   Exibir: Registro da tentativa realizado
            |
            v
           Fim
```

**Explique como você faria a Depuração Manual.**

Eu leria o código linha por linha e o executaria mentalmente, utilizando os métodos de Teste de Mesa e Fluxo de Execução para me guiarem.

**Somente ao final, escreva qual seria a lógica correta.**

```javascript
let cadastro = true
let mensalidade = true
let vaga = true
let bloqueado = true

if (cadastro && mensalidade && vaga && !bloqueado){
    console.log("Matrícula liberada")
}else{
    console.log("Matrícula negada")
}

console.log("Registro da tentativa realizado")
```




# 📈 Sprint Review — Semana 03

## 🧠 Etapa 1 — Revisão das Anotações

### Estruturas de Repetição

### 1. O que é uma estrutura de repetição?

-> São estruturas que nos permitem executar determinadas instruções de um bloco de código um número finito de vezes, sem precisar escrevê-las várias vezes.

Basicamente, vamos falar para o sistema repetir uma ação enquanto a condição for verdadeira.

---

### 2. Qual é o principal objetivo de utilizar uma repetição em um algoritmo?

-> Facilitar e automatizar tarefas repetitivas. Sendo assim, o código fica mais limpo e facilita a manutenção.

---

### 3. O que é uma condição de parada e por que ela é importante?

-> É a condição que freia o loop. Como o nome já diz, é a condição que monitora e diz quando é a hora do loop parar. A falta dela cria um loop infinito.

---

## `while`

### 4. O que é o `while`?

-> `while` é uma estrutura de repetição que executa um bloco de código **enquanto** a condição for verdadeira.

---

### 5. Em quais situações o `while` é mais indicado?

-> É mais utilizado quando não sabemos exatamente quantas vezes o sistema vai se repetir. Basicamente, quando quem decide se vai repetir ou não é o usuário.

---

### 6. Quais são os três elementos fundamentais de um `while`?

Tudo começa na condição, onde o sistema pergunta se é `true` ou `false`.

Depois temos o bloco de código que vai ser executado.

E, por último, mas não menos importante, a atualização da variável de controle para não criarmos um loop infinito.

---

## `for`

### 7. O que é o `for`?

-> Esta é a estrutura de repetição que usamos quando sabemos quantas vezes o bloco irá se repetir.

Uma definição melhor seria:

> Para cada repetição, execute essa instrução até que a condição deixe de ser verdadeira.

---

### 8. Qual a principal diferença entre `for` e `while`?

-> Na minha opinião, é o caso de uso, onde o `while` utilizamos quando não sabemos quantas vezes irá se repetir, e o `for` para quando sabemos.

Além disso, temos uma diferença na estrutura na hora de escrever o código, onde as três partes são escritas separadamente no `while` e, no `for`, todas acontecem na mesma linha.

---

### 9. O que representam as três partes do cabeçalho do `for`?

```javascript
for (inicialização; condição; atualização)
```

Explique cada uma delas.

- **Inicialização** = Cria a variável de controle.
- **Condição** = É a pergunta que o sistema faz, a condição para ele executar o código.
- **Atualização** = Atualiza a variável de controle para que o loop não se torne infinito.

---

## `do...while`

### 10. O que é o `do...while`?

-> Estrutura de repetição que permite que o bloco seja executado pelo menos uma vez, mesmo que a condição seja inicialmente falsa.

Basicamente ele diz:

> Execute isso... enquanto.

Como a condição vem só depois, ele executa o bloco primeiro.

---

### 11. Qual é a principal diferença entre `while` e `do...while`?

-> Permitir que o bloco seja executado pelo menos uma vez, mesmo que a condição seja inicialmente falsa.

---

### 12. Em quais situações o `do...while` costuma ser mais adequado?

Quando o sistema precisa que um determinado bloco seja executado pelo menos uma vez. Um exemplo pode ser um menu.

---
# ⚡ Etapa 2 — Revisão Ativa

### 1. Qual estrutura você escolheria para mostrar os números de 1 até 100? E por quê?

-> **`for`**, pois sei exatamente onde deve ser o fim do meu loop.

---

### 2. Qual estrutura utilizaria para um sistema de login onde não sabemos quantas tentativas o usuário fará?

Por quê?

-> **`while`**, pois quem decide quantas vezes vai repetir é o usuário.

---

### 3. Qual estrutura utilizaria para um menu que obrigatoriamente precisa aparecer pelo menos uma vez?

-> **`do...while`**, pois ele é mais apropriado quando precisamos executar pelo menos uma vez, mesmo que a condição seja inicialmente falsa.

---

### 4. O que causa um loop infinito?

-> A falta da atualização da variável de controle, impedindo que a condição se torne falsa.

---

### 5. Observe o algoritmo:

```javascript
let i = 1;

while (i <= 5) {

    console.log(i);

}
```

Qual é o erro?

-> Erro lógico. Faltou a atualização da variável, causando um loop infinito.

---

### 6. Observe:

```javascript
for (let i = 5; i <= 10; i--) {
```

Por que esse algoritmo nunca termina?

-> Gera um loop infinito, pois a atualização está subtraindo de `i`, enquanto a condição é `i <= 10`. Como `i` continua diminuindo, a condição continuará sendo verdadeira para sempre.

---

### 7. Observe:

```javascript
do {

    console.log("Olá");

} while (false);
```

Quantas vezes a mensagem será exibida?

Explique.

-> Apenas uma vez, mesmo que a condição seja falsa, pois o `do...while` permite que isso aconteça, já que a verificação ocorre somente depois da execução do bloco.